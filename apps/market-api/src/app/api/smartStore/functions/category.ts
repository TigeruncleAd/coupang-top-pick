'use server'

export async function getCategories() {
  // const token = await getTokenSmartStore()
  // const response = await fetch(`${SMART_STORE_HOST}/v1/categories`, {
  //   headers: {
  //     Authorization: `Bearer ${token}`,
  //   },
  // })
  // const data = await response.json()
}
